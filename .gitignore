.DS_Store
__pycache__/
*.pyc

# Local Codex runtime entrypoints. These are machine-specific symlink mounts.
.codex/
.agents/

# Local-only experiment logs and machine-specific notes.
local/

# Local dashboard state. Generated outputs should live under ~/.codex/dashboard,
# but keep these ignored in case someone runs the tool from the repo.
dashboard/config.toml
dashboard/preset-state.json
dashboard/index.html
dashboard/dist/
